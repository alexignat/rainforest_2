<h1>All Products</h1>

<% @products.each do |product| %>
  <p><%= link_to product.name, product %>, <%= product.description %>, <%= product.price_in_cents %></p>
<% end %>

<%= link_to "Add a product", new_product_path %>